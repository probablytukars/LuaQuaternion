name: CI/CD
on:
  push:
    branches: [ "main" ]

jobs:
  Integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run tests
        run: binaries/ubuntu/luau tests/test.luau
      - name: Build code
        run: binaries/ubuntu/lua54 tests/build.lua
  Deployment:
    runs-on: ubuntu-latest
    steps:
      - name: Install Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16'

      - name: Install moonwave
        run: npm i -g moonwave
      
      - name: Configure github
        run: |
          git config --global user.email "89352810+probablytukars@users.noreply.github.com"
          git config --global user.name "probablytukars"
          git remote set-url origin https://github.com/probablytukars/repo.git -t gh-pages
      
      - name: Create documentation
        run: moonwave build --publish
